using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraCustom : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(TransparencySortMode.Perspective);
    }
}
